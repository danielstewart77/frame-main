# Installed into /opt/frame/venv inside the sandbox image, not the control
# plane's venv — the channel shim is the only thing in the container that needs
# Python packages, and it runs as a subprocess of the harness.
mcp>=1.2
httpx>=0.27
